---
import DocsLayout from '../../layouts/DocsLayout.astro';

const linux = `# Linux x86_64 (native) + aarch64 (cross)
sudo apt install build-essential protobuf-compiler cmake clang \\
                 libclang-dev mold lld gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
cargo build -p easytier-ffi --release
CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER=aarch64-linux-gnu-gcc \\
CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_RUSTFLAGS=-Clink-arg=-fuse-ld=lld \\
CC_aarch64_unknown_linux_gnu=aarch64-linux-gnu-gcc \\
cargo build -p easytier-ffi --release --target aarch64-unknown-linux-gnu`;
---

<DocsLayout title="Building" description="Build the mod and its native libraries.">
  <p>The mod needs the EasyTier FFI native (<code>easytier_ffi</code>) per platform, under
    <code>src/main/resources/natives/&lt;os&gt;-&lt;arch&gt;/</code>. Build with Rust 1.95 + <code>protoc</code> + <strong>mold</strong>.</p>

  <h2>Natives</h2>
  <pre><code>{linux}</code></pre>
  <p>Windows: <code>cargo build -p easytier-ffi --release</code> → <code>easytier_ffi.dll</code>.
    macOS must be built on a Mac (Apple SDK).</p>

  <p><strong>Windows <code>Packet.dll</code> shim.</strong> <code>easytier_ffi.dll</code> load-time-imports
    Npcap's <code>Packet.dll</code>. etmc runs <code>no_tun</code>, so it never calls those functions — but the
    import must resolve or the loader fails the whole library (the old "provide a <code>Packet.dll</code>"
    error). We ship a tiny shim exporting exactly those symbols and pre-load it, so no Npcap install is
    needed. Regenerate it whenever you rebuild the Windows native:</p>
  <pre><code>{`uv run --with pefile python tools/gen_packet_shim.py \\
  common/src/main/resources/natives/windows-x86_64/easytier_ffi.dll \\
  common/src/main/resources/natives/windows-x86_64/Packet.dll`}</code></pre>

  <h2>The jar</h2>
  <pre><code>{`./gradlew copyNatives   # copy the Windows dll into resources
./gradlew build         # -> build/libs/etmc-<version>.jar`}</code></pre>

  <h2>CI</h2>
  <p>
    <code>.github/workflows/build.yml</code> builds the native for all five targets (Windows, Linux x64/arm64,
    macOS x64/arm64) on their own runners and assembles a jar bundling them — this is how macOS natives are
    produced.
  </p>

  <h2>This site</h2>
  <pre><code>{`cd site
bun install
bun run dev        # local dev
bun run build      # static build -> dist/
bun test           # wire-format vectors (must match the mod's WireFormatTest)
bun run deploy     # astro build && wrangler deploy`}</code></pre>
  <p>
    The in-browser generator writes the <code>etmc://</code> link, the <code>ETMC1:</code> code and the
    config TOML by hand — a second implementation of what the mod's <code>JoinCode</code> /
    <code>Toml</code> produce. <code>bun test</code> and the mod's <code>WireFormatTest</code> assert the
    same frozen vectors, so if you change either encoder, change both and run both.
  </p>
</DocsLayout>
